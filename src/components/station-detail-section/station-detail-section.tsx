import React from "react";
import type { Trip } from "../../server/trpc/models/trip";

interface Props {
    trip: Trip
}

export function StationDetailSection(props: Props): React.ReactElement {

    const { trip } = props;

    const 

    return (
        <>
           content
        </>
    );
}